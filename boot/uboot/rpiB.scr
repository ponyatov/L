echo ==================
#usb start
#dhcp
#tftp ${kernel_addr_r} /rpiBclock.kernel
tftp ${kernel_addr_r} /kernel.uimg
#tftp ${fdt_addr_r} /bcm2708-rpi-b.dtb
#tftp ${kernel_addr_r} /rpiBclock.kernel.uimg
#bootm ${kernel_addr_r} - ${fdt_addr_r}
setenv bootargs console=ttyAMA0,115200 kgdboc=ttyAMA0,115200 console=tty1 ip=dhcp
bootm ${kernel_addr_r}  
echo ==================
