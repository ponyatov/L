echo ==================
usb start
dhcp
setenv ncip 192.168.111.1
setenv stdout nc
setenv stderr nc
tftp ${kernel_addr_r} /rpiBclock.kernel
#tftp ${kernel_addr_r} /kernel.img
tftp ${fdt_addr_r} /bcm2708-rpi-b.dtb
bootz ${kernel_addr_r} - ${fdt_addr_r}  
echo ==================
